# esp32s3_devkit firmware

Board-local firmware for `esp32s3_devkit` belongs here.

Consume `hardware/contracts/esp32s3_devkit.yaml` as the stable hardware handoff for pin naming, interfaces, and artifact metadata.
