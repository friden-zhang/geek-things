# Interfaces Packages

Put reusable connector definitions, buses, and board-level interface abstractions here.

Use this directory when a circuit fragment defines how boards talk to each other or how firmware identifies pins, lanes, or channels.
