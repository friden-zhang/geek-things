# app

Initial atopile board project migrated from the repository root scaffold.

## Build

```bash
ato build
```

Run the command from `hardware/boards/app/`.

## Companion Files

- contract: `hardware/contracts/app.yaml`
- firmware target: `firmware/boards/app/`
