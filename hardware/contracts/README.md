# Board Contracts

Each board contract is the stable interface between hardware design, firmware, and host-side tooling.

Keep these files descriptive and tool-friendly so they can be consumed directly today and generated from later if needed.
